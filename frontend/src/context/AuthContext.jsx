import { createContext, useContext, useEffect, useState } from 'react'; import { get, send } from '../services/api';
const Auth = createContext(null); export function AuthProvider({ children }) { const [user, setUser] = useState(null); const [checking, setChecking] = useState(true); useEffect(() => { if (!localStorage.getItem('rcdf_token')) return setChecking(false); get('/auth/profile').then(setUser).catch(() => localStorage.removeItem('rcdf_token')).finally(() => setChecking(false)); }, []); const login = async (credentials) => { const result = await send('post', '/auth/login', credentials); localStorage.setItem('rcdf_token', result.data.token); setUser(result.data.user); }; const logout = () => { localStorage.removeItem('rcdf_token'); setUser(null); }; return <Auth.Provider value={{ user, checking, login, logout, setUser }}>{children}</Auth.Provider>; } export const useAuth = () => useContext(Auth);
