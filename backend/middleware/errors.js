export function notFound(req, res) { res.status(404).json({ success: false, message: `Route not found: ${req.method} ${req.path}` }); }
export function errorHandler(error, req, res, next) { console.error(error); if (error.name === 'ZodError') return res.status(400).json({ success: false, message: 'Please check the submitted fields.', errors: error.flatten() }); if (error.code === 11000) return res.status(409).json({ success: false, message: 'That value is already in use.' }); res.status(error.statusCode || 500).json({ success: false, message: 'Something went wrong. Please try again.' }); }
